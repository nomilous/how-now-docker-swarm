const CONCURRENT = 1;
const kue = require('kue');
const queue = kue.createQueue({
  redis: {
    host: 'swarm-redis'
  }
});

queue.process('request', CONCURRENT, function(job, done){

  done(null, {job_result: {
    worker_id: job.workerId,
    job: job.data
  }});

});
